import { Bounce } from "react-toastify";

export const notifyOptions = {
	autoClose: 2000,
	position: "top-center",
	closeOnClick: true,
	pauseOnFocusLoss: false,
	pauseOnHover: true,
	transition: Bounce,
};
